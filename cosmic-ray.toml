[cosmic-ray]
module-path = ["dogfood/lru-cache/lru_cache.py"]
timeout = 30.0
excluded-modules = []
test-command = "python3 -m unittest discover -s dogfood/lru-cache -t dogfood/lru-cache -v"

[cosmic-ray.distributor]
name = "local"

[cosmic-ray.filters.git-filter]
branch = "main"
