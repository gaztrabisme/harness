# Wiki Index

Claude's memory for the **Personal Dev Harness** project — the navigable layer over the detailed
`research/` docs. Read this → `active-work.md` at the start of every session.

## Architecture
- [architecture.md](architecture.md) — **THE MAP (start here when lost).** System diagram (four crates + the align→run→land flow), then the concepts: three planes, five pillars, the work spine, model-per-role. Points to `research/00-design-v2.md` for full detail.

## Decisions
- [decisions.md](decisions.md) — Resolved decisions + rationale, the open foundation decision (language/runtime, under eval), and Rejected Approaches.

## Active Work
- [active-work.md](active-work.md) — Current workstream: the **Foundation Eval** (DR1 omp / DR2 beads) that gates Phase 0.

## Assessment
- [readiness-assessment.md](readiness-assessment.md) — **Re-assessed 2026-07-15** (June original preserved below it). June's "not ready" verdict RETIRED on live evidence: critical path built + validated on the PDSI real dogfood (5/5 landed, real-ONNX slice) and the self-hosting sprint; every gate fired live and held. **New verdict: ready as the *project* driver (CC contained as worker); ad-hoc work stays interactive CC — that split is the posture, not a shortfall.** New critical path: (1) hygiene+granularity slice, (2) draft grounding, (3) reviewer fix-or-drop (0/2 calibration, gated by a number), (4) **measured daily-driver trial** — the claim graduates on numbers, not documents. Confusion bounce parked on trigger; harden-vacuity-on-templates ruled doctrine.

- [trial-ledger.md](trial-ledger.md) — **The instrument for the daily-driver claim** (critical-path item 4). Per-ticket rows: operator-min, worker cost, gate catches/misses, bounces, reviewer adjudication. Columns + decision rules pinned before ticket 1 (2026-07-15); reviewer fix-or-drop resolves at 5 trial tickets. Stream: PDSI continuation. Status: **1/5 landed** (t6 notification pipeline `f55fa6d`).

## Log
- [log.md](log.md) — Chronological record of sessions.

## Detailed research (raw sources, point-in-time)
The `research/` directory holds the deep findings the wiki summarizes:
- `00-design-v2.md` — the buildable design (partially superseded by the Rust-native direction; see decisions)
- `01..06` — breadth literature foundation (memory, spine, test-hardening, self-evolution, parallel, Pi internals)
- `07-pi-spike.md` — EXTEND-pi spike: Align gate validated at runtime
- `08-symphony-teardown.md` — coordinator mechanics
- `09-additions-orientation-and-plan.md` — the 13 external refs + verdicts
- `10-eval-scope.md` — the scoped foundation eval
- `11..16` — omp/beads teardowns, foundation decision, author constellation, Phase-0 scope
- `17-telemetry-and-memory-plane.md` — **telemetry + memory designed together; telemetry slice scoped first** (pre-adversarial-review; the next build)
- `18-memory-plane.md` — **memory pillar: design + 4-reviewer adversarial review folded in** (CoALA split; sliced A=lexical/B=vector-gated/C=decay+reflection-gated). **Slice A BUILT** — `crates/board/src/memory.rs` (FTS5 lexical sidecar: `remember`/`recall`/`recall_body`, migration v2). **P0 auto-context priming BUILT** (§3 loop integration) — closes the dormant compounding loop: `recall_primed` (body-carrying, all-scopes, token-overlap floor, non-bump, on a shared `recall_rows`) wired into `run_ticket` auto-injects explore's reflection-on-kill post-mortems into every worker's prompt (`PrimedHit`, `MEMORY_RECALL_K`, `### Relevant past experience` section). **S1 t5-floor fix + S3 researcher-subagent BUILT (read-side, research/31)** — see the `31-eval-results.md` entry below.
- `28-memory-retrieval-eval.md` — **Memory Slice B (vector leg) GO/NO-GO gate: plan + 4-reviewer adv review (§10, demolished the planned full eval) + DESCOPE + execution verdict (§12)**. The pre-registered research/18 §10.5 gate, run as a full pillar workflow. Adv review's 5 convergent killers (wrong SUT `recall_primed`-vs-floorless-`recall`; provenance-as-relevance bias; underpowered ~13% @ N≤40; tautological EXACT/PARAPHRASE split; disproportionate) → descoped to a **3-arm hand probe** (14-row artifact corpus + 14 paraphrase queries through `recall_primed`/floorless `recall`/jina-v5 dense — arm 2 isolates floor-vs-dense). **Result recall@5: primed 12/14 · floorless 13/14 · dense 14/14. Verdict: Slice B NO-GO** (floorless handles 13/14; dense's 1/14 confined to zero-token-overlap paraphrases). Embedding columns stay dormant-but-warmed (build-not-vendor on a future GO). Follow-up → board t5 (floor over-filters single-content-token paraphrases; BM25-score-aware fix, left todo by design — noise side unmeasured). EXERCISE only, nothing landed/committed.
- `29-rust-native-retrieval-survey.md` — **Rust-native retrieval-engine survey (L4 leg of the Slice B project)**. Dicklesworthstone constellation + field scan: frankensearch (mine-for-design — Tantivy duplicates the FTS5 heavy dep, in-process ONNX is the wrong shape for HTTP oMLX, license-rider ambiguity), fast_vector_similarity (skip — rank-correlation not cosine, no license file), cass_memory_system / coding_agent_session_search (design-only, already folded into research/18 §10.3). **Nothing to vendor even on a GO**; field delta confirms the design isn't stale.
- `31-eval-results.md` — **memory S1 t5-floor eval + S3 researcher-subagent design/probe/build (the full memory read-side workflow)**. **t5 (BUILT):** 36-row mined corpus + 57 provenance-labelled queries settle the `recall_primed` floor by a number — ship **a\*1** (strip stopwords via `content_tokens`, keep `need≥1`): noise 18→13/24, recovery held 32/33, no tuned constant. The original "floor too strict" premise is FALSE on a real corpus (the miss was a 14-row-probe artifact); the real defect was **precision**. BM25-score floor + strict need≥2 both REJECTED by their own pre-registered gates (decisions.md "t5"). **S3 (BUILT):** memory-researcher subagent — a local-LLM deep-research loop that explores the store (`search_memory`→`recall_body`→return a selected `{used_ids, context}` packet) and feeds only relevant lessons back. Live behavior probe confirmed the 35B drives the loop with only a **safe** failure mode (redundant calls → runs out of turns, never wrong content). Built in `crates/agent/src/researcher.rs` (pure core: `ContextPacket`/`parse_packet`/`select_lessons`/`RESEARCHER_SYSTEM`/`TOOL_CALL_CAP`) + `run_memory_researcher` glue in main.rs (oMLX-pinned, 25s timeout, two READ-ONLY board tools, dedup + cap-hit synthesis); **default-OFF behind `HARNESS_MEMORY_RESEARCHER`, falls back to `recall_primed` on any empty/failure/timeout** (byte-identical when OFF). Gate: 12 units + workspace green + clippy clean (production) + `#[ignore]` live oMLX smoke PASSED (selected the relevant lesson over a distractor, 15.15s). **Ship-eval DEFERRED** per scope B — the BM25 corpus is saturated, a +15pt ship gate is unclearable until the store accumulates real lessons (same posture as the dormant embedding cols). EXERCISE only, nothing committed.
- `30-deepseek-provider.md` — **DeepSeek V4 as a second cloud backend (doc-verified + live-confirmed)**. Wired into `OpenAiProvider` as a second dialect (`Dialect::DeepSeek`) alongside oMLX. Records the wire facts: base `https://api.deepseek.com` (OpenAI-compatible), models `deepseek-v4-pro`/`deepseek-v4-flash`, 1M context / 384K output, the `thinking` object knob (`{type, reasoning_effort: high|max}` — **no numeric budget**, unlike oMLX's `thinking_budget`), `reasoning_content` returned (must be stripped before re-send), tools≤128, prompt caching automatic, pricing table, and the **stale-doc trap** (`/guides/reasoning_model` describes the legacy `deepseek-reasoner`, wrong for V4). Selection via `HARNESS_PROVIDER=deepseek`; only the landing `run` path is selectable, `explore` stays oMLX-pinned. **Secret handling:** the key lives ONLY in `DEEPSEEK_API_KEY` env, never hardcoded/filed/in-trajectory. Gate: unit `body_carries_deepseek_thinking` + operator-run `#[ignore]` live smoke. Nothing committed.
- `32-board-close-path.md` — **board-lifecycle Fix B design: terminal reachability for non-landing tickets**. `Done` was reachable ONLY via `Land→Done` (GATE_LANDED), stranding non-code completions (latent) and abandonments (live — t3). Recommended package (adv-reviewed → SHIP-WITH-CHANGES, all 3 MAJOR folded): **one new edge `Review→Done` + one human gate `GATE_RESOLVED` + a `close` verb**, no schema migration. Both invariants held — keystone (resolved is human-only → agent can't self-close) + no-skip (only Review→Done added; Todo/InProgress/Verify/Align→Done stay structurally illegal). Records the 3 forks (single Done vs Cancelled terminal; all-kinds vs non-code-only; Review-only close point) + rejected approaches (Cancelled-now = migration cost; wildcard any→Done = breaks structural no-skip; close-refuses-code = strands t3). **BUILT (#64)** — spine.rs edge+gate, main.rs `close <id> "<note>" [--abandon]`; dogfooded live (t3 review→done, resolved row present + zero landed rows). Paired with **Fix A (#63)** — `align()` bridges Rework→Align so the rework loop reconverges. See decisions.md "Board-lifecycle".
- `19-parallel-exploration-plan.md` — **parallel-exploration pillar: design + adv review folded in (§9), scoped to a PROBE**. Fan out N diverse workers across isolated worktrees, rank by objective signal, report a winner candidate — reaps/consolidates/lands nothing. **BUILT** — `crates/agent/src/explore.rs` (pure selection core) + `run_explore` in main.rs.
- `20-self-evolution-plan.md` — **self-evolution pillar: design + adv review at design AND pre-land folded in (§9), scoped to Half A only**. Found **the open compounding loop** (lessons written, never read). Half A = the loop reads the committed, human-approved `wiki/case-law.md` and injects it into every worker's prompt; Half B (reflect/oMLX-distill/critic-panel) deferred to a measured trigger (empty pond + unsafe-as-specified). **BUILT** — `crates/agent/src/evolve.rs` (pure read-path core: section-aware harvest + flag/drop gate-weakening screen + budget) + `load_case_law`/`build_system_prompt` glue in main.rs + `wiki/case-law.md`.
- `21-think-stripping-and-budget.md` — **verbose-local-model sprint: scope + 3-lens adv review + RE-SCOPE folded in (§9)**. Headline feature `strip_think` **CUT** — reviewer-C live probe proved the served Qwen emits **zero `<think>` tags** (reasons in plain prose) → a stripper is dead code. Real leak = uncapped re-fed prose+tool-output, not tags. **BUILT** — `crates/agent/src/refeed.rs` (pure `cap`/`assembled_size` + 7 tests) + bounded re-feed + `[ctx]` overflow signal + `agent trajectory --full` in main.rs. Exercise (throwaway `/tmp` Thompson-NFA, never in repo history) surfaced a **third failure mode: reasoning-loop collapse** (model repeated ~6 paragraphs 20–22× verbatim until length-cutoff) + a gap (write_file content rides in **uncapped** tool_call arguments).
- `22-loop-gate.md` — **Tier-1 loop gate: discussion (literature grounding + "are we tunnel-visioned on the local provider?") + scope + necessity-first adv review folded in (§9)**. Builds the **cross-turn tool-call cycle** stop (the canonical agent loop), keyed strictly off **universal** signals (`resp.tool_calls` + `resp.stop_reason`) — the discipline test that keeps it model-agnostic, not provider tunnel vision. **BUILT** — `crates/agent/src/loopgate.rs` (pure `signature` + `LoopGate` state machine: byte-identical-consecutive detection, run-level strikes → **nudge on strike 1, stop on strike 2**, new `looped` outcome label) + wiring in `run_ticket`. **Wiring execution-tested** via a provider-injection refactor (`run_ticket` takes `&dyn Provider`) + a `ScriptedProvider` mock driving the real loop (2 integration tests assert `looped`/iters/nudge end-to-end). Intra-turn text-repetition (the §21 collapse), Tier-2 streaming abort, and period-2/window detection all **deferred** (Wu Wei, §8).
- `23-local-verbosity.md` — **local-model verbosity (Axis-B) research: capability verification + 3-axis literature/framework/empirical survey + recommendation**. Thesis: oMLX must prove a cheap local model can get work done, so verbosity robustness is in scope. **Verified oMLX wire knobs** (per-request, on `/v1/chat/completions`): `chat_template_kwargs:{enable_thinking:false}` (thinking fully OFF), the flat `thinking_budget:N` (the real field — `reasoning.max_tokens`/`thinking.budget_tokens` are ignored), `guided_grammar`/`structured_outputs`/`response_format`, full sampling. Programmatic profiles exist (admin-authed) but are not needed. **Falsified** two prior assumptions (grammar-JSON-overrun-detection is dead — reasoning is separated into `reasoning_content`; "move off native tool-calling" is counter-signaled — both ref frameworks keep it). **Token-elasticity trap** reproduced: a tight-but-nonzero `thinking_budget` relocates the spiral into `content`, saves nothing. **Function-calling finding:** think-OFF is strictly better for single-step dispatch (3–15× fewer tokens, same/better correctness). **RECOMMENDATION: reactive escalation** (default think-OFF, escalate to think-ON on `Length`/loopgate-strike/gate-red) — **NOT** a predictive effort-router, **NO** graded `thinking_budget` ladder. **Residual MEASURED (2026-06-10, §7):** on generative code think-OFF silently fails ~**30%** (near-miss artifacts, no signal) while naive think-ON is unusable (`max_tokens` does NOT bound reasoning → ran to 8091 tok/830 s); **the downstream `tests_green` artifact gate is the catch**, so the recommendation is validated & sharpened (escalation must be `thinking_budget`-bounded; router stays shelved — a triage call can't see a content-level near-miss). **DEFAULT half BUILT (2026-06-10):** `think: bool` on `provider::Request` (loop sets it false every turn); Qwen `enable_thinking` mapping quarantined in `OpenAiProvider::request_to_body` (unit-tested); reactive escalation documented but deferred (downstream gate is the safety net). Build/clippy clean, suite green (+`body_carries_enable_thinking`). Nothing committed.
- `25-context-engineering.md` — **context-engineering survey (Hybrid: Claude local teardown + 3 parallel web workers → synthesis + adv review)**. Answers Gary's re-feed question: today = whole unpruned transcript + crude byte cap, **zero semantic curation** (§1). §2 local teardown (oh-my-pi + pi-mono, **L1–L13** catalog) + §3 web (OpenCode/Hermes/Claude Code/Aider/literature). **Headline: 4-codebase convergence on the same 7-section structured summary; universal mechanism = bounded recency (protect head+tail) + structured summary of the middle + offload/clear tool output; Lost-in-the-Middle (TACL 2024) is the why. No source does semantic curation (tempers Decision 2).** §4 our 6 gaps → fixing subset **F1–F5**; reasoning-refeed reconciled with Decision 3. §5 adv review: **a context mechanism may be lossy but NEVER silently lossy** (the invariant). §6 → 4 slices, built AFTER bounded-thinking. **F1 BUILT (2026-06-10):** `refeed::assemble(messages, head_keep, keep_recent_tokens)` — immutable head (system prompt + task spec) + ~20K-tok verbatim tail + explicit elision marker for the dropped middle; tail boundary walked **backward** past leading tool_results so a call is never orphaned from its result (the §5 invariant). Wired into the loop (replaces `messages.clone()`); stale `CONTEXT_WARN_BYTES` 32K-window phantom → token-based reasoning-boundary warn (`COMPACT_TRIGGER_TOKENS=32K`). Budget from research/27 §6. **F4 BUILT (2026-06-10):** `refeed::cap_tool_calls` bounds re-fed tool-call *arguments* (the uncapped `write_file`-body gap the research/21 exercise surfaced) with the same head+tail+marker `cap` — lossy-but-never-silent; id/name preserved; execution path untouched (uses raw `resp.tool_calls`, so files still write in full). agent 74/1-ignored (+7 across F1+F4), clippy clean. **F2 BUILT (2026-06-10):** token-threshold compaction — `refeed::should_compact`/`compaction_span`/`apply_compaction` (pure; share `tail_boundary` with `assemble` so view+fold never disagree) + `compact_transcript` in the loop. Fires off the FULL transcript (not the capped view) > `HARNESS_COMPACT_TRIGGER` (default 32K); folds the middle into a `SUMMARIZER_SYSTEM` **7-section summary** placed at the head boundary → **cumulative** folds (§2.8, no separate bookkeeping). §5 hard rules wired: abort+loud on summarizer error / empty / no-progress (`after<before` guard), 2-strike breaker disables compaction for the run, `assemble` floor still bounds every turn on any abort. **Live oMLX gate PASSED:** real A3B folded 48 msgs **~8861→~735 tok (12×)**, all 7 sections, exact path + unanswered question preserved verbatim (§2.6) — validates the context-engineering thesis on the local model. **F5 (file touch-list) largely subsumed** by the summary's Files section. agent 81/2-ignored, clippy clean. **F3 BUILT (2026-06-10):** tool-output offload — `read_file` gained optional `offset`/`limit` (line-window, `slice_lines`, whole-file default unchanged) as the targeted re-read primitive; `refeed::should_offload` + `offload_preview` (pure) + `ARTIFACT_DIR`; `write_artifact` (in `main.rs`) writes the full output to `<worktree>/.harness/artifacts/tool-<seq>.txt` and the loop pushes a head/tail preview **+ a mandatory re-read hint** naming the exact relative path (lossy in-window, lossless on disk — §5). Write-failure degrades to inline `cap` (still non-silent) + WARN. **Integration-safe (verified):** `.harness/` gitignored ⇒ artifacts never land (`git add -A` skips), never falsely dirty the worktree (`git status --porcelain` skips) so the plan→execute gate is unaffected, and die with the worktree on land; relative path ⇒ confined `read_file` accepts it. Gate: deterministic end-to-end (buried line in a 4000-line output offloaded → recovered via paginated confined `read_file`) + pagination + 2 unit tests; **no live oMLX** (correctness is mechanical, not model-behavior — the deterministic test is the gate). Tail-bias flip deferred (Wu Wei). agent 85/2-ignored, clippy clean. **The F1–F5 context-engineering build subset is COMPLETE** (F5 subsumed by F2's Files section); deferred: NoLiMa-lite empirical probe + tail-bias measurement.
- `27-context-budget.md` — **context-budget sizing (Hybrid: local teardown + KB + parallel web workers → synthesis + adv review)**. Sibling of 25 (25 = how to curate the middle; 27 = how big the window should be). Triggered by Gary's challenge to a baked-in 32K wall. **Verified the 32K cap is a phantom** — oMLX serves **256K-native** (`max_context_window: 262144`). But "256K-native" is a *retrieval* claim, not a *reasoning* one: a coding agent reasons multi-hop, and reasoning erodes far earlier than recall (NoLiMa two-hop 57→26% by 32K; RULER effective ≈ ½ advertised; Qwen3-30B-A3B → ~64K effective). **Verdict (§6):** size to the **A3B reasoning curve** — effective window **48K**, hard window 40K, **compaction trigger 32K**, reserve 16K, keepRecent 20K, token-est chars/4, **KV fp16** (no 4-bit at ≤48K). Sets the research/25 F1 tail budget (~20K tok) + F2 trigger; fix the `CONTEXT_WARN_BYTES` docstring. NoLiMa-lite oMLX probe deferred as empirical gate. Research only; nothing built.
- `26-bounded-thinking.md` — **bounded-thinking pillar: scope + 4-reviewer adv review + root-cause correction + SHIPPED wire change**. Reverses research/23's think-OFF default to **reasoning ON-but-bounded** (Decision 3). §7 adv review (convergent) **CUT** Gary's predictive self-estimating budget triage (downside-only under a quality objective; self-prediction is unreliable). §9 owns a fabricated-causality error (claimed `thinking_budget` was a dead no-op + invented a "model swap"; **real cause = two per-model server flags** `thinking_budget_enabled` + `reasoning_parser` in `~/.omlx/model_settings.json`, read live). §9.1 verifies post-flip: `thinking_budget:N` is a **flat top-level SOFT target** (not a hard cap, no exhaustion signal, `max_tokens` is the backstop), bounded think-ON is loop-safe. §10 **SHIPPED:** `provider::Request.think: bool → think_budget: Option<u32>`; `openai.rs` maps `None`→off / `Some(n)`→flat budget; `agent` preset ladder low/med/high=1024/2048/4096, `DEFAULT_THINK_BUDGET=2048`, env `HARNESS_THINK_BUDGET`. "Who picks the rung" RESOLVED = fixed per-turn default; caller-tagged + reactive escalation are additive follow-ons. 90 tests green, clippy clean, live smoke green.
- `24-plan-execute-nudge.md` — **plan→execute nudge (the no-action stall): scope + 2-reviewer adv review (§10) + BUILT fold-in (§11)**. Fixes the **fourth failure mode** (the diagnostic spine's last gap): the model emits a plan in prose, calls **no tool**, and the natural-stop branch banks it `completed` (Unit B wrote a plan + 0 files). Disjoint sibling of the loop gate (ToolCalls branch vs no-tool-call branch). **BUILT** — `crates/agent/src/planexec.rs` (pure `verdict(acted, code, nudged) → Accept|Nudge|Stall`; 1-nudge bound, monotone) + wiring in `run_ticket`. **BLOCKER fix:** `acted` = **worktree-dirty at stop** (`git status --porcelain`, new `git::is_dirty`), NOT tool-based — the system prompt orders `bash ls -R` first so a tool proxy is true every run. New `stalled` outcome label (no migration; forced at call site, `looped` wins; `rank_outcomes` already sinks it). **Reviewer-A split:** ship the label unconditionally, gate the nudge on a live number — and the number says the nudge **did not convert** (the 35B re-asserted completion), so the **label is the value, the nudge is on probation**. agent 67/1-ignored (+9: 5 unit matrix + 4 `ScriptedProvider` integration), clippy clean, live oMLX run-3 produced a real `stalled` row. **The four-failure-mode spine is now complete** (refeed/loopgate/[deferred collapse]/planexec).
